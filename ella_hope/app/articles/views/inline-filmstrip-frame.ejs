<li data-resource-id="<% if (item !== null) { %><%= item.id %><% } %>" data-filmstrip-frame-index-li="<%= index %>">
	<i class="glyphicon glyphicon-remove pull-right remove-filmstrip-frame"></i>
	<h4><%= $.t('articles.frame_number')%> <%= index + 1 %></h4>

	<div class="form-group">
		<label class="control-label" for="photo<%= index %>"><%= $.t('articles.photo')%></label>
		<div class="controls photo<%= index %> form-group">
			<%
			var titlePhotoInserted = false;
			if (item && item.photo && item.photo.public_url) {
				titlePhotoInserted = true;
			} %>
			<div class="title-photo-empty<%= index %>" <%= titlePhotoInserted === false ? '' : 'style=display:none' %>>
				<button class="btn btn-default add-filmstrip-frame-photo" data-filmstrip-frame-index="<%= index %>" ><%= $.t('articles.add_photo')%></button>
			</div>
			<div class="title-photo<%= index %> title-photo-img" <%= titlePhotoInserted === true ? '' : 'style=display:none' %>>
				<img src="<%= item && item.photo ? item.photo.public_url : '' %>" />
				<button class="btn btn-default remove-filmstrip-frame-photo" data-filmstrip-frame-index="<%= index %>"><%= $.t('articles.remove_photo')%></button>
			</div>
			<input type="hidden" name="photo<%= index %>" value="<%= item && item.photo ? item.photo.resource_uri : '' %>" />
			<span class="help-inline"></span>
		</div>
	</div>

	<div class="control-group form-group js-textrea-box">
		<label class="control-label required" for="content<%= index %>"><%= $.t('articles.content') %></label>
		<div class="controls">
			<textarea class="content form-control" rows="10" id="filmstrip-frame-content<%= index %>" name="content<%= index %>" placeholder="<%= $.t('articles.add_content')%>"><% if (item !== null) { %><%= item.content %><% } %></textarea>
			<span class="help-inline"></span>
		</div>
		<div class="box-snippet hero-unit pull-right"></div>
	</div>
</li>