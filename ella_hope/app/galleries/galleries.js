steal(

)
.then(

	/**
	 * Shows the galeries.
	 * This control inherits almost everything from Articles control.
	 * @tag controllers, home
	 */
	Galleries = Articles(
	/* @static */
	{
		defaults: {
			model: 'galleries'
		}
	},
	/* @prototype */
	{

	})
);
